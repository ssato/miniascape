description: RHEV Manager
hostname: &hostname rhev-manager-1
fqdn: &fqdn rhev-manager-1.m2.local
ip: &ip 192.168.155.31
interfaces:
  - device: eth0
    mac: "52:54:00:05:00:31"
    ip: *ip
    host: *hostname
    fqdn: *fqdn
    network: service

rhev:
  # If remote:
  # db_host: *fqdn
  # db_remote_pass: xxxxxxxxx
  auth_pass: redhat0123
  db_local_pass: redhat0123
  override_iptables: no
  #dc_type: ISCSI

