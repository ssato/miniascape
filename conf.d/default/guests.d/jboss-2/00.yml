description: JBoss Server
hostname: &hostname jboss-2
fqdn: &fqdn jboss-2.m2.local
ip: &ip 192.168.155.42
interfaces:
  - device: eth0
    mac: "52:54:00:05:00:42"
    ip: *ip
    host: *hostname
    fqdn: *fqdn
    network: service

jboss:
  servers:
    - name: *hostname
      group: group1

