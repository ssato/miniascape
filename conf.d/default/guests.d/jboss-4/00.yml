description: JBoss Server
hostname: &hostname jboss-4
fqdn: &fqdn jboss-4.m2.local
ip: &ip 192.168.155.44
interfaces:
  - device: eth0
    mac: "52:54:00:05:00:44"
    ip: *ip
    host: *hostname
    fqdn: *fqdn
    network: service

jboss:
  servers:
    - name: *hostname
      group: group2

