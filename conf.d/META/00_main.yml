guest:
  subdir: guests.d
  files:
    - "common/*.yml"
    - "sysgroups.d/%(name)s/*.yml"
    - "guests.d/%(name)s/*.yml"

network:
  subdir: networks.d
  files:
    - "common/*.yml"
    - "networks.d/%(name)s.yml"

storage:
  subdir: storages.d
  files:
    - "common/*.yml"
    - "storages.d/%(name)s.yml"

