#! /bin/bash
set -e

prefix=@prefix@
exec_prefix=@exec_prefix@
libexecdir=@libexecdir@

for net in net-1 net-2 net-3; do
    chkconfig --del dnsmasq-$net
    $libexecdir/@PACKAGE@/net-unregister $net   
done
