# see also:
#
# * RHEL 6 LVS Admin Guide, 4.4. GLOBAL SETTINGS: http://red.ht/V8X5FE
#

* Primary server public IP:
* Primary server private IP (may be blank):

* TCP Timeout [0]:
* TCP Fin Timeout [0]:
* UDP Timeout [0]:

* Use network type (NAT or Direct Routing):


# LVS-NAT:

* NAT Router IP:
* NAT Router netmask:
* NAT Router device (e.g. eth0:1):


# 

* Redundant server public IP:
* Redundant server private IP (may be blank):

* Heartbeat Interval (seconds):
* Assume dead after (seconds):
* Heartbeat runs on port [539]:

* Use Sync Daemon:
* Sync Daemon Interface:
* Sync daemon id:


# Real Servers:

* Name:
* Application port:
* Protocol:
* Virtual IP Address:
* Virtual IP Network Mask:
* Firewall Mark:
* Device:
* Re-entry Time:
* Service Timeout:
* Quiesce server:
* Load monitoring tool:
* Scheduling:
* Persistence:
* Persistence Network Mask:

## Real Server Subsection:

* Name:
* Address:
* Weight:

## MONITORING SCRIPTS:

* Sending Program:
* Send:
* Expect:
















