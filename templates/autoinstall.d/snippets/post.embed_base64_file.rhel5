{% macro embed_base64_file(outdir="/root") -%}
test -d {{ outdir }} || mkdir -p {{ outdir }}
cat << EOF > {{ outdir }}/setup.tar.xz.base64
{% include 'setup.tar.xz.base64' %}
EOF
cd {{ outdir }} && \
base64 -i -d setup.tar.xz.base64 > setup.tar.xz && \
xz -d setup.tar.xz && \
tar -xC {{ outdir }}/ setup.tar && cd -
{%- endmacro %}
