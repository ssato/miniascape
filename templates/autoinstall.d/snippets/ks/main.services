{% block services -%}
{%-    if services is defined -%}
services
{{-       " --enabled %s" % services.enabled|join(',') if services.enabled -}}
{{-       " --disabled %s" % services.disabled|join(',') if services.disabled -}}
{%-    endif %}
{% endblock %}
